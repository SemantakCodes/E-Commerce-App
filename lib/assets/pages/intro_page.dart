import 'package:flutter/material.dart';

class IntroPage extends StatelessWidget {
  const IntroPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold
    (
      body: Column
      (
        children: 
        [
          //LOGO
          Image.asset("/lib/assets/logo/nikeLogo.png"),
          //TITLE

          //SUBTITLE

          //START NOW BUTTON
        ],
      ),
    );
  }
}